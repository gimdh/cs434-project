package com.gimdh.project.slave.sampler

import java.io.FileReader

import scala.io.Source
import com.google.protobuf.wrappers.UInt64Value

import scala.reflect.io.File

object Sampler {
  def apply(reader: FileReader) = {

  }
}